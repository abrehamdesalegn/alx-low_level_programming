#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * _strdup - check the code for ALX School students.
 *
 * Return: Always 0.
 * @str: para
 */
char *_strdup(char *str)
{
	char *ar;

	ar = (char *)malloc(sizeof(char));
	ar = str;
	return (ar);
}
